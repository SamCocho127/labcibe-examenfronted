# Copia este archivo como .env para desarrollo local (no se sube a GitHub).
# En Netlify no hace falta .env: el proxy de netlify.toml apunta a MonsterASP.

# URL pública del backend (opcional en local; vacío usa proxy de vite.config.ts).
# VITE_API_URL=http://investigation2prograiv.runasp.net

VITE_REPORTS_URL=/reportar-estafa
VITE_REPORTS_LIST_URL=/reportes
